# node
node_modules/
dist/
.vite/
*.log
npm-debug.log*

# python
__pycache__/
*.py[cod]
.venv/
venv/
services/compute/.venv/
.pytest_cache/

# env
.env
.env.local

# os / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# local-only notes (not for the repo)
SUMMARY.md

# generated voxel-model assets (regenerable via services/compute/app/build_chest.py).
# Models are committed so GitHub Pages can serve them — EXCEPT hires_shoulder, whose
# 254 MB .mat.bin exceeds GitHub's 100 MB per-file limit (kept local-only).
services/compute/data/
apps/web/public/models/hires_shoulder/*.mat.bin
apps/web/public/models/hires_shoulder/*.glb
