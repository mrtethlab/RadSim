fastapi>=0.110
uvicorn[standard]>=0.29
numpy>=1.26
python-multipart>=0.0.9
# GPU engine — install the CUDA build explicitly (plain `pip install torch` gives CPU-only):
#   pip install --index-url https://download.pytorch.org/whl/cu128 torch
torch>=2.5
